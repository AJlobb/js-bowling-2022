class Game{
constructor() {
this.players.push(new Player("AJ"))
this.players.push(new Player("Tom"))
}
players = []
playerTurn = 0
frame = 0

ballRolled = (pins) => {
/**
 * 
 * find player based on player turn. 
 * incriment frame score by pins
 * if last ball for playerFrame incrient player turn
 * if playerturn is  > player.length set playerTurn to 0 and incriment frame. 
 * if frame > 10  game over
 */

}
}

}